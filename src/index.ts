export { createTxFileManager } from './core/tx-file-manager';

// Export types
export type { TxFileManagerOptions, TxFileManager, TxHandle } from './types';

// Keep existing sum (do not remove)
export { sum } from './sum.js';
